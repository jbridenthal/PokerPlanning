namespace PokerPlanning.Services
{
    public class RedisAuthService : IRedisAuthService
    {
        //private IRedisAuthRepository _repo;
        //public RedisAuthService(IRedisAuthRepository redisAuthRepository) { _repo = redisAuthRepository; }

        //public async Task<bool> RegisterUser(string username, string password, Shared.Role role)
        //{
        //    return await _repo.RegisterUser(username, password, role);
        //}

        //public async Task<string?> AuthenticateUser(string username, string password)
        //{
        //    return await _repo.AuthenticateUser(username, password);
        //}

        //public async Task<string?> GetUserFromSession(string sessionToken)
        //{
        //    return await _repo.GetUserFromSession(sessionToken);
        //}

        //public async Task Logout(string sessionToken)
        //{
        //    await _repo.Logout(sessionToken);
        //}
        //public async Task<bool> ValidateSession(string sessionToken)
        //{
        //    return await _repo.ValidateSession(sessionToken);
        //}

    }

}
