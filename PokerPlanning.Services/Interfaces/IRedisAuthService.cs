using PokerPlanning.Shared;

namespace PokerPlanning.Services.Interfaces
{
    public interface IRedisAuthService
    {
        //Task<string?> AuthenticateUser(string username, string password);
        //Task<string?> GetUserFromSession(string sessionToken);
        //Task Logout(string sessionToken);
        //Task<bool> RegisterUser(string username, string password, Role role);
        //Task<bool> ValidateSession(string sessionToken);
    }
}